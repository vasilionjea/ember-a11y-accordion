{{yield (hash
  item=(component 'accordion-item'
    isCollapsible=(readonly isCollapsible)
    register=(action 'registerItem')
    toggle=(if isCollapsible (action 'toggleItem') (action 'showItem'))
  )
)}}
